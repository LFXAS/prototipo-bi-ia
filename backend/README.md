# Backend

FastAPI contiene por ahora configuración, sesión PostgreSQL y endpoints de salud. Los paquetes restantes delimitan los módulos que se implementarán en iteraciones posteriores.

Todo comando debe ejecutarse mediante Docker desde la raíz del repositorio.
